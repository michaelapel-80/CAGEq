# Build output (DLL, obj, import lib) — reproducible from build.bat.
build/

# Endpoint effect-chain backups written by scripts/register.ps1. Machine-specific, and
# they encode a real endpoint GUID; unregister.ps1 consumes them locally.
scripts/fx-backup-*.reg
