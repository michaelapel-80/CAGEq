{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "scope",
  "description": "Capability for the detached vectorscope window — it only listens to the global scope events and manages its own window.",
  "windows": ["scope"],
  "permissions": ["core:default"]
}
