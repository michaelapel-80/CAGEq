# CAGEq DSP sidecar dependencies.
#
# Python 3.10 is required: autoeq 4.1.2 pins 2022-era numpy/matplotlib whose wheels
# only exist through cp310 (3.11+ would try to compile them and fail). Set up with:
#
#   py -3.10 -m venv .venv
#   .venv/Scripts/python -m pip install -r python/requirements.txt
#
# autoeq resolves its own pinned numpy/scipy/pandas/matplotlib/etc.
autoeq==4.1.2
